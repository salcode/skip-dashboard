{
	"name": "salcode/skip-dashboard",
	"description": "WordPress plugin to do the following: On login, users with access load the the backend wp-admin Posts list instead of the dashboard.  Those users who do not have access to that page (e.g. subscribers), load the dashboard as they always have.",
	"type": "wordpress-plugin",
	"homepage": "https://github.com/salcode/skip-dashboard",
	"license": "GPL-2.0+",
	"require": {
		"composer/installers": "~1.0"
	}
}
